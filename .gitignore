# Secrets — never commit local config (DB creds, live Ollama tunnel URL, carrier keys)
api/config.local.php

# Runtime artifacts (notification logs, generated label CSVs, receipts)
storage/*
!storage/.htaccess
!storage/labels/
!storage/logs/
!storage/labels/.gitkeep
!storage/logs/.gitkeep
*.log

# OS / editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Deployment archives
*.zip
*.tar.gz